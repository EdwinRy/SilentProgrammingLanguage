/*
func int32 cd(int32 x)
{
    if(x == 0)
    {
        return 0;
    }

    return cd(x - 1);
}
*/


func int64 fib(int64 x)
{
    if(x == 0){ return 0;}
    if(x == 1){ return 1;}
    return fib(x - 1) + fib(x - 2);
}


func int64 main()
{
    int64 b = fib(40);
}
