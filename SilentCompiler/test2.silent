/*
func int64 countdown(int64 x)
{
    if(x > 1)
    {
        return countdown(0);
    }
    return 420;
}

func void main()
{
    int64 k = 9+10;
    k = countdown(6);
}
*/

func int64 fibonacci(int64 x)
{
    if(x > 2)
    {
        return fibonacci(x - 1) + fibonacci(x - 2); 
    }
    else
    {
        return 1;
    }
    
}

func int64 main()
{
    int64 x = fibonacci(10);
}
