struct vec3
{
    int64 x;
    int64 y;
    int64 z;
}


func int64 main()
{
    int64 b;

    vec3 v1 = new vec3;
    v1.x = 9;
    v1.y = 10;
    v1.z = 2;

    v1.z = v1.x + v1.y;
    b = v1.z;
    //delete v1; //Segmentation fault if commented out
    v1.x = 5;
    b = v1.x;
}
